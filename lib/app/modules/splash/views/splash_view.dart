import 'package:flutter/material.dart';

class SplashMobilePortrait extends StatelessWidget {
  const SplashMobilePortrait({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}